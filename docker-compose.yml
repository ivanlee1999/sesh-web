services:
  sesh-web:
    build: .
    container_name: sesh-web
    restart: unless-stopped
    user: "1000:1000"
    ports:
      - "3033:3000"
    env_file:
      - .env.local
    volumes:
      - /mnt/nas/docker/sesh-web/data:/app/data
