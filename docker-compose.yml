services:
  sesh-web:
    build: .
    container_name: sesh-web
    restart: unless-stopped
    ports:
      - "3033:3000"
    env_file:
      - .env.local
    volumes:
      - sesh_data:/app/data

volumes:
  sesh_data:
